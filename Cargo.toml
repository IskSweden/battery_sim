[package]
name = "battery_simulator"
version = "0.1.0"
edition = "2024"

[dependencies]
calamine = "0.23"
chrono = {version = "0.4", features = ["serde"]}
serde = {version = "1.0", features = ["derive"]}
csv = "1.3"
anyhow = "1.0"